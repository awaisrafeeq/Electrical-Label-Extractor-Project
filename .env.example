# Choose provider
VISION_PROVIDER=openai

# Models
OPENAI_MODEL=gpt-4o
ANTHROPIC_MODEL=claude-3-haiku-20240307

# API keys (configure in Render dashboard → Environment)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: if you prefer path-based poppler config (usually not needed on Render)
# POPPLER_PATH=/usr/bin
